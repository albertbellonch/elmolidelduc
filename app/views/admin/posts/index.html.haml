%h1 Lista de noticias
.separations

%table
  %tr
    %th Título
    %th Fecha
    %th Usuario
    %th Comentarios
    %th Publicado?
    %th{ :colspan => 3 }

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.date
      %td= post.user.email
      %td= post.comments.size
      %td= post.published ? "Sí" : "No"
      %td= link_to 'Mostrar', [:admin, post]
      %td= link_to 'Editar', edit_admin_post_path(post)
      %td= button_to 'Eliminar', admin_post_path(post), :confirm => 'Estás seguro?', :method => :delete

.show_and_newlink= link_to 'Nueva noticia', new_admin_post_path
